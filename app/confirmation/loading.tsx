export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-slate-50">
      <p className="text-sm font-medium uppercase tracking-[0.2em] text-slate-500">Loading confirmation…</p>
    </div>
  );
}
